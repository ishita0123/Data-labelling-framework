To make a docker containers:
1. cd to the folder containing spam.py
2. In terminal, write the following command:  docker build -t spam1 .
 (This will build the docker image).
 
 3. To run this image: docker run spam1 
 
 
